const InputValidation = {
    firstNameAll: /^[A-Z][A-Za-z-]{2,}$/,
    lastNameAll: /^[A-Z][A-Za-z-]{3,}$/,
    allowedChar: /[A-Za-z-]{3,}$/,
    fisrtUpper: /[A-Z]{3,}$/,
};

export default InputValidation;
